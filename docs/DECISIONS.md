# Decisions log

One line per non-obvious choice: what, why, alternative rejected.

- 2026-06-09 — Build at the repo root (`True-Research/` is the project root) — §2's `marathon-research/` is the project folder itself, and nesting adds path depth for nothing; rejected: a `marathon-research/` subdir inside the repo.
- 2026-06-09 — Renamed `CLAUDE (2).md` → `CLAUDE.md` (git mv) — §2 names the file `CLAUDE.md` and the space-laden name breaks tooling; rejected: keeping a duplicate copy (divergence risk).
- 2026-06-09 — Project venv at `.venv/` for all deps — system pip is Debian-managed and aborted on a PyJWT uninstall; rejected: `--ignore-installed` into system site-packages (fragile, pollutes the container).
- 2026-06-09 — Engine sessions will always pass `setting_sources=[]` + an explicit `system_prompt` string — SDK default (None) loads user/project/local settings and CLAUDE.md, violating the amnesia invariant (§3.1); rejected: relying on SDK defaults.
- 2026-06-09 — Secrets are loaded from `.env` into the frozen Settings object and injected per spawned session via `options.env`; never exported into the driver's `os.environ` — child processes inherit parent env and `options.env` cannot *remove* inherited keys, so a process-global key would leak to local-endpoint sessions; rejected: `load_dotenv()` into process env.
- 2026-06-09 — Budget breakers trust `ResultMessage.total_cost_usd` (client-side estimate per docs) — breakers need a conservative tripwire, not billing-grade truth; rejected: polling the Usage & Cost Admin API (extra dep, latency, admin-key requirement).
- 2026-06-09 — State hash = SHA-256 over sorted (relpath, file-sha) pairs of `open_questions.yaml` + `findings/` only (per §3.5) — hashing the whole run dir would let PROGRESS/ledger writes mask a stall; rejected: hashing the entire run dir.
- 2026-06-09 — Sessions return structured output (`output_format` json_schema); engine code performs ALL state-file mutations and withholds Write/Edit from every session — invariants 3/6 become code-enforced; rejected: letting models write YAML/JSON state via Write tool (format drift corrupts state).
- 2026-06-09 — `created_by` gains "worker" — §6 has workers spawning child questions on fragmentation, §4's enum lacked it; rejected: mislabeling worker children as evaluator-created.
- 2026-06-09 — Fragmented questions: parent marked resolved without a finding, children carry it forward, decision logged; rejected: leaving parent open (worker would re-pick it forever).
- 2026-06-09 — Ledger recording moved into the session layer (base.run_role_session records success AND failure) — a session that errors after spending tokens still accounts its spend; rejected: driver-side recording from returned results (loses spend on session exceptions).
- 2026-06-09 — `cached_tokens` = cache_read + cache_creation input tokens summed; `usd` from total_cost_usd covers the rate difference; rejected: separate columns (schema §4 has one field).
- 2026-06-09 — Output schemas avoid numeric min/max JSON-schema constraints (unsupported by structured outputs); ranges (priority 1-5, confidence 0-1, credibility 0-100) validated in engine code with typed errors; rejected: silent clamping.
- 2026-06-09 — Synthesizer with zero findings writes an honest "nothing to report" report with NO model call; rejected: paying Opus to say nothing.
- 2026-06-09 — Evaluator pass while unresolved questions remain is force-failed by the engine with a logged decision (belt-and-braces on invariant 2); rejected: trusting the model's passed flag alone.
- 2026-06-10 — Worker target selection prefers orphaned in_progress questions over open ones — in_progress at cycle start can only mean a crashed/errored prior attempt (every worker exit path moves the status), and the smoke run showed an orphan starving behind open questions; rejected: open-first with in_progress fallback.
- 2026-06-10 — Local-models work pulled forward from Phase 4 into Phase 3 (user-directed) — reader fan-out is the cost lever and §1's hybrid posture names reader_subagent as the local role; rejected: keeping local in Phase 4 while burning Sonnet on page reading.
- 2026-06-10 — Readers are ENGINE-spawned sessions surfaced to the worker as an in-process MCP tool (read_source) — SDK in-session subagents inherit the parent's endpoint env, so per-role endpoint routing requires separate spawns; rejected: AgentDefinition subagents (no endpoint mixing).
- 2026-06-10 — Readers get NO tools: the engine fetches pages (httpx + HTML strip) and hands text in — sidesteps local-model tool-calling unreliability (§1) and the unverified WebFetch-vs-local behavior; rejected: WebFetch inside reader sessions.
- 2026-06-10 — Structured output is endpoint-dependent: API output_format on first-party, prompted-JSON + engine-side parse on non-first-party (output_format support cannot be assumed there); rejected: assuming local endpoints honor output_format.
- 2026-06-10 — Non-first-party ledger entries force usd=0 (§1) while keeping real token counts; rejected: trusting the CLI price table for models it cannot know.
- 2026-06-10 — Evaluator may CLOSE unresolved questions as immaterial (logged per close) so fail-and-deepen converges; rejected: append-only question lists (runs sprawl past every breaker).
- 2026-06-10 — VERIFIED: host-broker sandboxes (Claude Code on the web) pin spawned-session auth and ignore injected ANTHROPIC_API_KEY/AUTH_TOKEN — in-container runs bill the sandbox, hybrid must not run in such sandboxes; scripts/check_local_backend.py wire-checks auth through a recording proxy; rejected: assuming documented env precedence holds in every environment.
- 2026-06-10 — Model-weight hosts (ollama.com, registry.ollama.ai, huggingface.co) are 403-blocked in this container — real-Ollama verification deferred to the user's machine via scripts/check_local_backend.py; routing/parse/ledger path verified here against a wire-accurate mock; rejected: calling local "done" without any endpoint-level verification.
- 2026-06-10 — Academic connectors implemented as engine-owned in-process MCP tools over public APIs (PubMed eutils, OpenAlex, arXiv) with pure unit-tested parsers; rejected: third-party paper-search MCP packages (supply-chain risk, untestable in this sandbox, heavier).
- 2026-06-10 — Visual captures flow through the same ReaderOutput/read-gate machinery as text reads (kind=page_capture, capture counts as a read); rejected: a parallel registration path for visual sources (two invariants to maintain).
- 2026-06-10 — vision_reader is a separate role (must stay vision-capable) instead of reusing reader_subagent — a local text model on readers must not silently take vision work; rejected: one shared reader role.
- 2026-06-10 — SearXNG chosen for the §1 MCP search fallback (fully-local, no key); Tavily/Exa deferred until wanted; deep-research-as-tool (§8 'optionally') skipped — extra vendor APIs, no in-container way to verify.
- 2026-06-10 — Per-endpoint price_per_mtok added; cost precedence = configured price (computed from tokens, cached billed at input rate) > first-party CLI estimate > free-local usd 0; fixes the §1 usd:0 rule blinding the budget breaker on PAID third-party endpoints; rejected: usd:0 for all non-first-party (wrong once you pay DeepSeek/Groq).
- 2026-06-10 — Retries (tenacity) only on TRANSIENT failures — transport errors + HTTP 408/429/5xx/529 — at both session spawns and engine fetches; permanent 4xx (403/404/validation) never retried, every retry logged; rejected: blanket retry (masks real failures, the thing §0 forbids).
- 2026-06-10 — Scheduler hook = driver --json-summary <path> (machine-readable run summary) per §8/§9 right-sizing; rejected: Temporal/Celery/queue infra (§9 out of scope).
- 2026-06-10 — Judge is a fresh Opus session scoring the §8 rubric, ledgered to a SEPARATE judge_ledger.json so eval scoring never contaminates run spend; deterministic metrics (cost/cycles/citation-resolution) come from run files not the model; rejected: folding judge cost into the run ledger.
- 2026-06-10 — Bake-off = per-invocation worker/reader model+endpoint overrides in run_evals.py, scores diffed across scores.json; rejected: hardcoded model matrix (config drift, can't add a candidate without code).
- 2026-06-10 — LangSmith-style tracing (§8 'if desired') skipped — PROGRESS.md + ledger.json + verdicts/ already form a complete on-disk trace; rejected: adding a tracing vendor dep for redundant capability.
