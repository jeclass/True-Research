# Runtime
claude-agent-sdk==0.2.95
pydantic>=2.7
PyYAML>=6.0
rich>=13.0
python-dotenv>=1.0
tenacity>=8.2
httpx>=0.27
ddgs>=9.0               # Docker-free DuckDuckGo search fallback (when SearXNG is down)

# Dev / test
pytest>=8.0
playwright>=1.40        # visual profile only — also run: playwright install chromium
scrapling[fetchers]>=0.4.9
rerankers[flashrank]>=0.10.0
